generator client {
  provider        = "prisma-client-js"
  output          = "../../api/node_modules/.prisma/client"
}

datasource db {
  provider = "sqlite"
}

model User {
  id    Int    @id @default(autoincrement())
  name String @unique
  comment String
}